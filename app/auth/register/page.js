import RegisterClient from './RegisterClient'

// page metadata for the register page 
export const metadata = {
    title: 'Create account',
}
export default function Page() {
    return <RegisterClient />
}